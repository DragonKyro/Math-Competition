# 2021 AIME I

## Problem 1
*Tags: casework, probability*

Zou and Chou are practicing their $100$-meter sprints by running $6$ races against each other. Zou wins the first race, and after that, the probability that one of them wins a race is $\frac23$ if they won the previous race but only $\frac13$ if they lost the previous race. The probability that Zou will win exactly $5$ of the $6$ races is $\frac mn$, where $m$ and $n$ are relatively prime positive integers. Find $m+n.$

<details><summary>Solution</summary>

**Setup.** Zou already wins race 1. Out of the remaining $5$ races, Zou must win $4$ and lose $1$. Let $W$ denote a race Zou won and $L$ a race he lost. The five possible win-loss patterns for races 2–6 are:
$$LWWWW, \quad WLWWW, \quad WWLWW, \quad WWWLW, \quad WWWWL.$$

**Key observation (how transitions contribute).** The probability of a race outcome depends only on who won the previous race:
- If Zou won the previous race, probability Zou wins the next is $\tfrac{2}{3}$ and probability Zou loses the next is $\tfrac{1}{3}$.
- If Zou lost the previous race, probability Zou wins the next is $\tfrac{1}{3}$ and probability Zou loses the next is $\tfrac{2}{3}$.

So to compute the probability of one pattern, walk through races 2 through 6, multiplying in the transition probability based on what happened the *previous* race (starting from Zou's known win in race 1).

**Step 1. Compute each pattern's probability.**

*Pattern $WWWWL$ (lose only the last race).* 
- Race 2: previous winner Zou, Zou wins $\to \tfrac{2}{3}$.
- Race 3: previous winner Zou, Zou wins $\to \tfrac{2}{3}$.
- Race 4: previous winner Zou, Zou wins $\to \tfrac{2}{3}$.
- Race 5: previous winner Zou, Zou wins $\to \tfrac{2}{3}$.
- Race 6: previous winner Zou, Zou loses $\to \tfrac{1}{3}$.

Product: $\left(\tfrac{2}{3}\right)^4 \cdot \tfrac{1}{3} = \tfrac{16}{243}$.

*Pattern $LWWWW$ (lose race 2).*
- Race 2: previous winner Zou, Zou loses $\to \tfrac{1}{3}$.
- Race 3: previous winner Chou, Zou wins $\to \tfrac{1}{3}$.
- Race 4: previous winner Zou, Zou wins $\to \tfrac{2}{3}$.
- Race 5: previous winner Zou, Zou wins $\to \tfrac{2}{3}$.
- Race 6: previous winner Zou, Zou wins $\to \tfrac{2}{3}$.

Product: $\left(\tfrac{1}{3}\right)^2 \cdot \left(\tfrac{2}{3}\right)^3 = \tfrac{8}{243}$.

*The three "interior loss" patterns ($WLWWW$, $WWLWW$, $WWWLW$) also give $\tfrac{8}{243}$ each.* Here's why: whenever Zou's one loss happens in an interior race (not race 6), there are exactly two "flip" transitions — the one where Zou loses after a win ($\tfrac{1}{3}$) and the one where Zou wins after a loss ($\tfrac{1}{3}$) — and three "stay" transitions where Zou wins after a win ($\tfrac{2}{3}$). So each contributes $\left(\tfrac{1}{3}\right)^2 \left(\tfrac{2}{3}\right)^3 = \tfrac{8}{243}$.

**Step 2. Add the probabilities.**
$$P = \underbrace{\tfrac{16}{243}}_{WWWWL} + \underbrace{4 \cdot \tfrac{8}{243}}_{\text{interior loss}} = \tfrac{16}{243} + \tfrac{32}{243} = \tfrac{48}{243}.$$

**Step 3. Reduce the fraction.** $\gcd(48, 243) = 3$, since $48 = 16 \cdot 3$ and $243 = 81 \cdot 3$. Thus
$$P = \frac{48}{243} = \frac{16}{81}.$$
Check $\gcd(16, 81) = 1$: $16 = 2^4$ and $81 = 3^4$ share no prime factors.

**Answer.** $m + n = 16 + 81 = \boxed{097}$.

</details>

---

## Problem 2
*Tags: pythagorean-theorem, similar-triangles*

In the diagram below, $ABCD$ is a rectangle with side lengths $AB=3$ and $BC=11$, and $AECF$ is a rectangle with side lengths $AF=7$ and $FC=9,$ as shown. The area of the shaded region common to the interiors of both rectangles is $\frac mn$, where $m$ and $n$ are relatively prime positive integers. Find $m+n$.
_(Asymptote diagram omitted — see AoPS for image)_

<details><summary>Solution</summary>

**Setup.** Rectangle $ABCD$ has $AB = 3$ and $BC = 11$; rectangle $AECF$ shares the diagonal $AC$ with $ABCD$ and has $AF = 7$, $FC = 9$. The shaded region is the parallelogram where the two rectangles overlap; we'll find its area.

**Key observation (shape of the overlap).** Let $G$ be the intersection of side $AD$ (of rectangle $ABCD$) with side $FC$ (of rectangle $AECF$). The overlap is a parallelogram with two opposite vertices at $A$ and $C$. Its "height" — measured perpendicular to side $AD$ — is the width of $ABCD$, namely $AB = 3$. So
$$[\text{shaded}] = 3 \cdot AG,$$
and we only need $AG$.

**Step 1. Similar triangles at $G$.** Two triangles meet at $G$:
- $\triangle AFG$: right angle at $F$ (from rectangle $AECF$), with leg $AF = 7$.
- $\triangle CDG$: right angle at $D$ (from rectangle $ABCD$), with leg $CD = AB = 3$.

Angles $\angle AGF$ and $\angle CGD$ are equal (vertical angles). So by AA, $\triangle AFG \sim \triangle CDG$. The ratio of similitude (matching the sides opposite equal angles) is
$$\frac{AF}{CD} = \frac{7}{3}.$$

**Step 2. Set up the equation.** Let $AG = x$. Since $AD = BC = 11$, $DG = 11 - x$. Using the similarity ratio $7 : 3$:
- $FG$ (in $\triangle AFG$) corresponds to $DG$ (in $\triangle CDG$), so $FG = \tfrac{7}{3}(11 - x)$.
- $CG$ (in $\triangle CDG$) corresponds to $AG$ (in $\triangle AFG$), so $CG = \tfrac{3}{7} x$.

Then $FG + GC = FC = 9$:
$$\tfrac{7}{3}(11 - x) + \tfrac{3}{7} x = 9.$$

**Step 3. Solve for $x$.** Multiply through by $21$ (the common denominator):
$$49(11 - x) + 9x = 189,$$
$$539 - 49x + 9x = 189,$$
$$539 - 40x = 189,$$
$$40x = 350,$$
$$x = \frac{35}{4}.$$

**Step 4. Compute the area.**
$$[\text{shaded}] = 3 \cdot \tfrac{35}{4} = \tfrac{105}{4}.$$
Check reduction: $105 = 3 \cdot 5 \cdot 7$ and $4 = 2^2$ share no primes, so $\gcd(105, 4) = 1$. Thus $m = 105$, $n = 4$.

**Answer.** $m + n = 105 + 4 = \boxed{109}$.

</details>

---

## Problem 3
*Tags: base-representations, combinatorics*

Find the number of positive integers less than $1000$ that can be expressed as the difference of two integral powers of $2.$

<details><summary>Solution</summary>

**Setup.** We want to count positive integers $n < 1000$ that can be written as $2^a - 2^b$ with integers $a > b \ge 0$.

**Step 1. Bound the exponents.** Since we need $2^a - 2^b > 0$ we need $a > b \ge 0$. Also, if $b \ge 1$ then $2^a - 2^b = 2(2^{a-1} - 2^{b-1})$, meaning *any* value that can be expressed with exponents $(a, b)$ (both $\ge 1$) can also be expressed by just the pair's "shape". But we're counting distinct *values*, so let's start by picking $(a, b)$.

If $a \ge 11$, then $2^a - 2^b \ge 2^{11} - 2^{10} = 2^{10} = 1024 > 1000$. So $a \le 10$. The allowed pairs have $0 \le b < a \le 10$.

**Step 2. Count valid pairs.** The number of pairs $(a, b)$ with $0 \le b < a \le 10$ is $\binom{11}{2} = 55$ (choose any two distinct values from $\{0, 1, \ldots, 10\}$ for $\{b, a\}$).

But some give $2^a - 2^b \ge 1000$. For $a = 10$:
- $2^{10} - 2^0 = 1023$, too big.
- $2^{10} - 2^1 = 1022$, too big.
- $2^{10} - 2^2 = 1020$, too big.
- $2^{10} - 2^3 = 1016$, too big.
- $2^{10} - 2^4 = 1008$, too big.
- $2^{10} - 2^5 = 992 < 1000$, OK.
- ...and larger $b$ give even smaller values, all OK.

For $a \le 9$: $2^a - 2^b \le 2^9 - 2^0 = 511 < 1000$, so all pairs are OK.

So $5$ pairs are too big; $55 - 5 = 50$ pairs remain.

**Step 3. All remaining values are distinct.** *Why?* Suppose $2^{a_1} - 2^{b_1} = 2^{a_2} - 2^{b_2}$. Rearrange: $2^{a_1} + 2^{b_2} = 2^{a_2} + 2^{b_1}$. Now use the fact that every positive integer has a unique binary representation. The left side, written in binary with no collisions, is a number with $1$s in positions $a_1$ and $b_2$ (or a single $1$ in position $a_1 + 1$ if $a_1 = b_2$); similarly on the right. Matching these up forces $(a_1, b_1) = (a_2, b_2)$ (checking both cases $a_1 = b_2$ and $a_1 \ne b_2$ gives only the trivial identification).

So each valid pair gives a distinct value of $n$.

**Answer.** $\boxed{050}$.

</details>

---

## Problem 4
*Tags: casework*

Find the number of ways $66$ identical coins can be separated into three nonempty piles so that there are fewer coins in the first pile than in the second pile and fewer coins in the second pile than in the third pile.

<details><summary>Solution</summary>

**Setup.** Let the three piles have $a, b, c$ coins with $a + b + c = 66$ and $1 \le a < b < c$. Count the number of such triples.

**Key observation (bound $a$).** Since $a < b < c$, we have $b \ge a + 1$ and $c \ge a + 2$, so
$$66 = a + b + c \ge a + (a+1) + (a+2) = 3a + 3,$$
giving $a \le 21$. Also $a \ge 1$.

**Step 1. For each fixed $a$, count $(b, c)$ pairs.** We need $a < b < c$ and $b + c = 66 - a$. Let $s = 66 - a$. We need integers $b, c$ with $a < b < c$ and $b + c = s$.

Given $b + c = s$ and $b < c$, we have $b < s/2 < c$, so $b$ ranges over integers with $\lfloor \frac{s-1}{2} \rfloor \ge b \ge a + 1$. The count is $\lfloor \frac{s-1}{2} \rfloor - a$ (when this is non-negative).

Let me make this concrete. We need $b$ strictly between $a$ and $s/2$. Since $b, c$ are integers:
- If $s$ is even, $b + c = s$ with $b < c$ means $b \in \{a+1, a+2, \ldots, s/2 - 1\}$. Count: $s/2 - 1 - a$.
- If $s$ is odd, $b \in \{a+1, a+2, \ldots, (s-1)/2\}$. Count: $(s-1)/2 - a$.

In both cases the count is $\lfloor (s-1)/2 \rfloor - a$ (as long as this is positive; otherwise $0$).

**Step 2. Tabulate for each $a$.** With $s = 66 - a$:

| $a$ | $s = 66-a$ | $\lfloor (s-1)/2 \rfloor$ | $\text{count} = \lfloor(s-1)/2\rfloor - a$ |
|---|---|---|---|
| $1$ | $65$ | $32$ | $31$ |
| $2$ | $64$ | $31$ | $29$ |
| $3$ | $63$ | $31$ | $28$ |
| $4$ | $62$ | $30$ | $26$ |
| $5$ | $61$ | $30$ | $25$ |
| $6$ | $60$ | $29$ | $23$ |
| $7$ | $59$ | $29$ | $22$ |
| $8$ | $58$ | $28$ | $20$ |
| $9$ | $57$ | $28$ | $19$ |
| $10$ | $56$ | $27$ | $17$ |
| $11$ | $55$ | $27$ | $16$ |
| $12$ | $54$ | $26$ | $14$ |
| $13$ | $53$ | $26$ | $13$ |
| $14$ | $52$ | $25$ | $11$ |
| $15$ | $51$ | $25$ | $10$ |
| $16$ | $50$ | $24$ | $8$ |
| $17$ | $49$ | $24$ | $7$ |
| $18$ | $48$ | $23$ | $5$ |
| $19$ | $47$ | $23$ | $4$ |
| $20$ | $46$ | $22$ | $2$ |
| $21$ | $45$ | $22$ | $1$ |

**Step 3. Sum the counts.** Reading off the table:
$$31, 29, 28, 26, 25, 23, 22, 20, 19, 17, 16, 14, 13, 11, 10, 8, 7, 5, 4, 2, 1.$$
Notice the pattern: the counts drop by $2$ then by $1$, alternating. Group them:
- Sum $= 31 + 29 + 28 + 26 + 25 + 23 + 22 + 20 + 19 + 17 + 16 + 14 + 13 + 11 + 10 + 8 + 7 + 5 + 4 + 2 + 1$.

Let me add systematically: $(31 + 1) + (29 + 2) + (28 + 4) + (26 + 5) + (25 + 7) + (23 + 8) + (22 + 10) + (20 + 11) + (19 + 13) + (17 + 14) + 16 = 32 + 31 + 32 + 31 + 32 + 31 + 32 + 31 + 32 + 31 + 16 = 5 \cdot 32 + 5 \cdot 31 + 32 + 16$.

Actually, let me just add straightforwardly:
$31+29 = 60$, $+28 = 88$, $+26 = 114$, $+25 = 139$, $+23 = 162$, $+22 = 184$, $+20 = 204$, $+19 = 223$, $+17 = 240$, $+16 = 256$, $+14 = 270$, $+13 = 283$, $+11 = 294$, $+10 = 304$, $+8 = 312$, $+7 = 319$, $+5 = 324$, $+4 = 328$, $+2 = 330$, $+1 = 331$.

**Answer.** $\boxed{331}$.

</details>

---

## Problem 5
*Tags: algebra, diophantine-equations*

Call a three-term strictly increasing arithmetic sequence of integers special if the sum of the squares of the three terms equals the product of the middle term and the square of the common difference. Find the sum of the third terms of all special sequences.

<details><summary>Solution</summary>

**Setup.** Let the three terms be $a - d$, $a$, $a + d$, where $a$ is the middle term and $d$ is the common difference. Since the sequence is strictly increasing, $d \ge 1$. All three terms must be integers, so $a, d$ are integers.

**Step 1. Translate the condition into an equation.** The sum of the squares of the three terms:
$$(a-d)^2 + a^2 + (a+d)^2 = (a^2 - 2ad + d^2) + a^2 + (a^2 + 2ad + d^2) = 3a^2 + 2d^2.$$
The condition "sum of squares equals middle term times (common difference)$^2$" is $3a^2 + 2d^2 = a \cdot d^2$.

**Step 2. Solve for $d^2$ in terms of $a$.** Rearrange:
$$3a^2 = ad^2 - 2d^2 = (a - 2) d^2 \implies d^2 = \frac{3a^2}{a - 2}.$$
For $d^2$ to be positive, we need $a - 2 > 0$, i.e., $a \ge 3$ (since $a$ is an integer).

**Motivation for next step.** We want $\frac{3a^2}{a-2}$ to be a perfect square. To progress, let me do polynomial division to strip out the "integer part":
$$\frac{3a^2}{a - 2} = \frac{3(a-2)^2 + 12(a-2) + 12}{a-2} = 3(a-2) + 12 + \frac{12}{a - 2} = 3a + 6 + \frac{12}{a - 2}.$$
(Check: $3a^2 = 3(a-2)^2 + 12(a-2) + 12$? Expand: $3(a^2 - 4a + 4) + 12a - 24 + 12 = 3a^2 - 12a + 12 + 12a - 24 + 12 = 3a^2$. Correct.)

**Step 3. Narrow down $a$.** For $d^2$ to be an integer, $(a - 2) \mid 12$. Since $a \ge 3$, $a - 2 \ge 1$, so $a - 2 \in \{1, 2, 3, 4, 6, 12\}$, giving $a \in \{3, 4, 5, 6, 8, 14\}$.

**Step 4. Test each value.** Compute $d^2 = \frac{3a^2}{a-2}$ and check if it's a perfect square:

| $a$ | $a - 2$ | $3a^2$ | $d^2 = 3a^2/(a-2)$ | perfect square? | $d$ |
|---|---|---|---|---|---|
| $3$ | $1$ | $27$ | $27$ | no | — |
| $4$ | $2$ | $48$ | $24$ | no | — |
| $5$ | $3$ | $75$ | $25$ | yes | $5$ |
| $6$ | $4$ | $108$ | $27$ | no | — |
| $8$ | $6$ | $192$ | $32$ | no | — |
| $14$ | $12$ | $588$ | $49$ | yes | $7$ |

So the valid $(a, d)$ pairs are $(5, 5)$ and $(14, 7)$.

**Step 5. Compute third terms and sum.** The third term is $a + d$.
- $(a, d) = (5, 5)$: third term $= 10$. Sequence: $0, 5, 10$. Check: $0 + 25 + 100 = 125$ vs $5 \cdot 25 = 125$. ✓
- $(a, d) = (14, 7)$: third term $= 21$. Sequence: $7, 14, 21$. Check: $49 + 196 + 441 = 686$ vs $14 \cdot 49 = 686$. ✓

Sum of third terms $= 10 + 21 = \boxed{031}$.

</details>

---

## Problem 6
*Tags: coordinate-geometry, cube-geometry*

Segments $\overline{AB}, \overline{AC},$ and $\overline{AD}$ are edges of a cube and $\overline{AG}$ is a diagonal through the center of the cube. Point $P$ satisfies $BP=60\sqrt{10}$, $CP=60\sqrt{5}$, $DP=120\sqrt{2}$, and $GP=36\sqrt{7}$. Find $AP.$

<details><summary>Solution</summary>

**Setup (coordinates).** Place $A$ at the origin and use the cube's edges as coordinate axes. Let the cube's side length be $s$; then $B = (s, 0, 0)$, $C = (0, s, 0)$, $D = (0, 0, s)$, and the opposite corner $G = (s, s, s)$. Let $P = (x, y, z)$.

**Step 1. Write out each distance squared.**
\begin{align*}
BP^2 &= (x-s)^2 + y^2 + z^2 = (60\sqrt{10})^2 = 36000, \\
CP^2 &= x^2 + (y-s)^2 + z^2 = (60\sqrt{5})^2 = 18000, \\
DP^2 &= x^2 + y^2 + (z-s)^2 = (120\sqrt{2})^2 = 28800, \\
GP^2 &= (x-s)^2 + (y-s)^2 + (z-s)^2 = (36\sqrt{7})^2 = 9072.
\end{align*}

**Step 2. Expand each.**
\begin{align*}
BP^2 &: x^2 + y^2 + z^2 - 2sx + s^2 = 36000, \\
CP^2 &: x^2 + y^2 + z^2 - 2sy + s^2 = 18000, \\
DP^2 &: x^2 + y^2 + z^2 - 2sz + s^2 = 28800, \\
GP^2 &: x^2 + y^2 + z^2 - 2s(x+y+z) + 3s^2 = 9072.
\end{align*}

Let $T = x^2 + y^2 + z^2$ and $S = x + y + z$. Rewriting:
\begin{align*}
T + s^2 - 2sx &= 36000, \tag{1}\\
T + s^2 - 2sy &= 18000, \tag{2}\\
T + s^2 - 2sz &= 28800, \tag{3}\\
T + 3s^2 - 2sS &= 9072. \tag{4}
\end{align*}

**Step 3. Add equations (1) + (2) + (3).**
$$3T + 3s^2 - 2s(x+y+z) = 36000 + 18000 + 28800 = 82800,$$
i.e., $3T + 3s^2 - 2sS = 82800$. Call this equation (5).

**Step 4. Subtract (4) from (5).** Notice both have $3s^2 - 2sS$. So
$$(5) - (4): \quad 3T - T = 82800 - 9072,$$
$$2T = 73728,$$
$$T = 36864.$$

**Step 5. Recover $AP$.** Since $A$ is the origin, $AP^2 = x^2 + y^2 + z^2 = T = 36864$. Thus
$$AP = \sqrt{36864}.$$
Factor: $36864 = 2^{12} \cdot 9 = 4096 \cdot 9$, so $\sqrt{36864} = 64 \cdot 3 = 192$.

**Answer.** $AP = \boxed{192}$.

</details>

---

## Problem 7
*Tags: casework, modular-arithmetic, trigonometry*

Find the number of pairs $(m,n)$ of positive integers with $1\le m<n\le 30$ such that there exists a real number $x$ satisfying $$\sin(mx)+\sin(nx)=2.$$

<details><summary>Solution</summary>

**Setup.** Since $\sin$ is bounded by $1$, the only way $\sin(mx) + \sin(nx) = 2$ is if both equal $1$. So we need some $x$ with $\sin(mx) = 1$ and $\sin(nx) = 1$.

**Step 1. Translate into angle conditions.** $\sin\theta = 1$ iff $\theta = \tfrac{\pi}{2} + 2\pi k$ for some integer $k$. So we need integers $a, b$ with
$$mx = \tfrac{\pi}{2} + 2\pi a \quad \text{and} \quad nx = \tfrac{\pi}{2} + 2\pi b.$$

**Step 2. Eliminate $x$.** Divide the second by the first (assuming $m, n \ne 0$):
$$\frac{n}{m} = \frac{nx}{mx} = \frac{\tfrac{\pi}{2} + 2\pi b}{\tfrac{\pi}{2} + 2\pi a} = \frac{1 + 4b}{1 + 4a}.$$
So the condition becomes: there exist integers $a, b$ with $\tfrac{n}{m} = \tfrac{4b + 1}{4a + 1}$.

**Step 3. Simplify.** The fractions $\tfrac{4b+1}{4a+1}$ have numerator and denominator both $\equiv 1 \pmod 4$. Multiplying through by $-1$, they're also allowed to both be $\equiv 3 \pmod 4$. Let $d = \gcd(m, n)$ and write $m = d m'$, $n = d n'$ with $\gcd(m', n') = 1$. Then $\tfrac{n}{m} = \tfrac{n'}{m'}$ in lowest terms.

So the condition "there exist $a, b$ with $\tfrac{n}{m} = \tfrac{4b+1}{4a+1}$" (where numerator and denominator of RHS are any integers $\equiv 1 \pmod 4$, not necessarily in lowest terms) is equivalent to: in the reduced fraction $\tfrac{n'}{m'}$, both $m'$ and $n'$ can be simultaneously $\equiv 1 \pmod 4$, OR both $\equiv 3 \pmod 4$ (after multiplying top and bottom by $-1$).

Since $\gcd(m', n') = 1$, neither can be even when the other is odd... actually both $m'$ and $n'$ must be odd (since if one were even, the other being $\equiv 1 \pmod 4$ wouldn't help — a fraction $\tfrac{\text{even}}{\text{odd}}$ in lowest terms can't equal $\tfrac{\text{odd} \equiv 1 \pmod 4}{\text{odd} \equiv 1 \pmod 4}$).

**Cleaner condition.** $(m, n)$ works iff, writing $d = \gcd(m, n)$ and $m' = m/d$, $n' = n/d$, both $m'$ and $n'$ are $\equiv 1 \pmod 4$, or both are $\equiv 3 \pmod 4$. Equivalently, $m$ and $n$ share the same power of $2$, and after dividing out that power of $2$, both are odd and congruent mod $4$.

**Step 4. Count.** Write $m = 2^p \cdot a$ and $n = 2^p \cdot b$ (same power $p$) with $a, b$ odd. The condition is $a \equiv b \pmod 4$. We'll group by the value of $2^p$:

*Case $p = 0$:* $m, n$ are both odd. Either both $\equiv 1 \pmod 4$ or both $\equiv 3 \pmod 4$.
- Odd numbers $\equiv 1 \pmod 4$ in $\{1, \ldots, 30\}$: $1, 5, 9, 13, 17, 21, 25, 29$ — that's $8$ numbers. Pairs: $\binom{8}{2} = 28$.
- Odd numbers $\equiv 3 \pmod 4$ in $\{1, \ldots, 30\}$: $3, 7, 11, 15, 19, 23, 27$ — that's $7$ numbers. Pairs: $\binom{7}{2} = 21$.

*Case $p = 1$:* $m = 2a$, $n = 2b$ with $a, b$ odd, both in $\{1, \ldots, 15\}$, same residue mod 4.
- $a \equiv 1 \pmod 4$: $a \in \{1, 5, 9, 13\}$, so $m \in \{2, 10, 18, 26\}$. $4$ numbers. Pairs: $\binom{4}{2} = 6$.
- $a \equiv 3 \pmod 4$: $a \in \{3, 7, 11, 15\}$, so $m \in \{6, 14, 22, 30\}$. $4$ numbers. Pairs: $\binom{4}{2} = 6$.

*Case $p = 2$:* $m = 4a$, $n = 4b$ with $a, b$ odd in $\{1, \ldots, 7\}$ (since $4 \cdot 7 = 28 \le 30$, $4 \cdot 9 = 36 > 30$), same residue mod 4.
- $a \equiv 1 \pmod 4$: $a \in \{1, 5\}$, so $m \in \{4, 20\}$. $2$ numbers. Pairs: $\binom{2}{2} = 1$.
- $a \equiv 3 \pmod 4$: $a \in \{3, 7\}$, so $m \in \{12, 28\}$. $2$ numbers. Pairs: $\binom{2}{2} = 1$.

*Case $p = 3$:* $m = 8a$ with $a$ odd, $m \le 30$ means $a \le 3$. So odd $a \in \{1, 3\}$ giving $m \in \{8, 24\}$, but these have different residues mod 4 ($8/8 = 1$, $24/8 = 3$), so no pairs.

*Case $p \ge 3$:* Too few numbers to form pairs with matching residue.

**Step 5. Add up.**
$$28 + 21 + 6 + 6 + 1 + 1 = 63.$$

**Answer.** $\boxed{063}$.

</details>

---

## Problem 8
*Tags: absolute-value, graphing*

Find the number of integers $c$ such that the equation $$\left||20|x|-x^2|-c\right|=21$$has $12$ distinct real solutions.

<details><summary>Solution</summary>

**Setup.** Let $f(x) = |20|x| - x^2|$. The equation is $|f(x) - c| = 21$, which splits into
$$f(x) = c + 21 \quad \text{or} \quad f(x) = c - 21.$$
So we need: (total real solutions of $f(x) = c + 21$) + (total real solutions of $f(x) = c - 21$) = $12$, with no overlap (the two $f$-levels $c + 21$ and $c - 21$ differ by $42$, so their solution sets are disjoint as long as both exist).

**Step 1. Understand the graph of $f$.** $f$ is even (since $f(-x) = f(x)$), so just study $x \ge 0$ and reflect.

For $x \ge 0$: $f(x) = |20x - x^2| = |x(20 - x)|$.
- On $0 \le x \le 20$: $x(20 - x) \ge 0$, so $f(x) = 20x - x^2 = -(x-10)^2 + 100$. This is a downward parabola with max $100$ at $x = 10$, and zeros at $x = 0$ and $x = 20$.
- On $x \ge 20$: $x(20 - x) \le 0$, so $f(x) = x^2 - 20x = (x - 10)^2 - 100$. This is an upward parabola, passing through $(20, 0)$ and increasing without bound.

Reflecting across the $y$-axis gives the shape on $x \le 0$. Summary of $f$'s graph:
- Zeros at $x = -20, 0, 20$.
- Two local maxima of height $100$ at $x = \pm 10$.
- A local min of $0$ at $x = 0$.
- Goes to $+\infty$ as $|x| \to \infty$.

**Step 2. Count solutions of $f(x) = y$ for each $y$.**
- $y < 0$: $0$ solutions ($f \ge 0$).
- $y = 0$: $3$ solutions ($x = -20, 0, 20$).
- $0 < y < 100$: by the graph's shape (three humps above the $x$-axis: two "arches" plus the two outer branches going to $\infty$), we get $6$ solutions — two on each arch (left arch, middle arch, right branch... wait, let's be careful).

Let me redo carefully. On $x \ge 0$ alone:
- For $0 < y < 100$: the middle arch $f(x) = 20x - x^2$ on $[0, 20]$ has $2$ solutions (the parabola's two intersections with $y$), and the right branch $f(x) = x^2 - 20x$ on $[20, \infty)$ has $1$ solution. Total: $3$ on $x \ge 0$.
- Doubling (by reflection): $6$ solutions for $0 < y < 100$.

- $y = 100$: each arch has exactly one solution ($x = 10$), and each right branch has one solution. On $x \ge 0$: $2$ solutions ($x = 10$ and the branch solution). Doubling: $4$.

- $y > 100$: no solutions on the middle arch (max $100$); each outer branch contributes one solution. Total $2$.

**Step 3. Find $c$ such that $f(x) = c - 21$ and $f(x) = c + 21$ together give $12$ solutions.**

The split must be $6 + 6$ (no other combinations work: $3 + \text{something}$ would require "something" = 9 which is impossible; $4 + \text{something}$ would need 8 which is impossible; $2 + \text{something}$ similarly).

So we need *both* $c - 21 \in (0, 100)$ *and* $c + 21 \in (0, 100)$.

From $0 < c - 21 < 100$: $21 < c < 121$.
From $0 < c + 21 < 100$: $-21 < c < 79$.

Intersection: $21 < c < 79$.

**Step 4. Count integers.** Integers $c$ with $21 < c < 79$ are $c \in \{22, 23, \ldots, 78\}$. Count: $78 - 22 + 1 = 57$.

**Answer.** $\boxed{057}$.

</details>

---

## Problem 9
*Tags: isosceles-trapezoid, similar-triangles, cyclic-quadrilaterals*

Let $ABCD$ be an isosceles trapezoid with $AD=BC$ and $AB<CD.$ Suppose that the distances from $A$ to the lines $BC,CD,$ and $BD$ are $15,18,$ and $10,$ respectively. Let $K$ be the area of $ABCD.$ Find $\sqrt2 \cdot K.$

<details><summary>Solution</summary>

**Setup.** Let $E$, $F$, $G$ be the feet of perpendiculars from $A$ to lines $BC$, $CD$, $BD$ respectively. So $AE = 15$, $AF = 18$, $AG = 10$. Let $AB = c$, $BC = AD = a$, $CD = b$. Since $ABCD$ is isosceles, it is cyclic.

**Key observation (set up similar triangles).** Using the similarities below, we'll find ratios between the sides of $ABCD$.

**Step 1. $\triangle ABE \sim \triangle ADF$.** Since $ABCD$ is cyclic (it's an isosceles trapezoid), $\angle ABC + \angle ADC = 180°$, so $\angle ABE = 180° - \angle ABC = \angle ADC = \angle ADF$. Both $\triangle ABE$ and $\triangle ADF$ are right triangles (right angles at $E$ and $F$), and they share this equal angle. So by AA, $\triangle ABE \sim \triangle ADF$, with
$$\frac{AB}{AD} = \frac{AE}{AF} = \frac{15}{18} = \frac{5}{6}.$$
Thus $AB = \tfrac{5}{6} AD$, i.e., $c = \tfrac{5}{6} a$, equivalently $a = \tfrac{6}{5} c$.

**Step 2. Express $BD$ in terms of $c$ using the area of $\triangle ABD$.** The area of $\triangle ABD$ can be computed two ways:
- Using base $AD$ and height $AE = 15$ (distance from... wait, $AE$ is the distance from $A$ to $BC$, not to $AD$. Let me fix this.)

Hmm — the distance from $A$ to line $BC$ is $15$. Drop the foot $E$ to line $BC$. Now, in $\triangle ABC$, using base $BC$ and altitude from $A$ to $BC$ (which is $AE = 15$), the area of $\triangle ABC$ is $\tfrac{1}{2} \cdot BC \cdot AE = \tfrac{1}{2} a \cdot 15$.

Actually in the isosceles trapezoid $\triangle ABD$ and $\triangle BAC$ are congruent (reflect across the perpendicular bisector of $AB$). So $[\triangle ABD] = [\triangle ABC] = \tfrac{1}{2} \cdot 15 \cdot a = \tfrac{15a}{2}$.

Also, $[\triangle ABD] = \tfrac{1}{2} BD \cdot AG = \tfrac{1}{2} \cdot BD \cdot 10 = 5 \cdot BD$.

Equating: $5 \cdot BD = \tfrac{15a}{2}$, so $BD = \tfrac{3a}{2}$.

**Step 3. Compute $AD$ using Pythagoras on the trapezoid.** Drop perpendicular from $A$ to $CD$ to get foot $F$ with $AF = 18$. Then $DF$ is the horizontal distance from $A$ to $D$, and by Pythagoras in $\triangle ADF$:
$$DF^2 + AF^2 = AD^2 \implies DF^2 = a^2 - 324.$$

In an isosceles trapezoid with parallel sides $AB = c$ (top) and $CD = b$ (bottom), symmetry gives $DF = \tfrac{b - c}{2}$. So $\tfrac{b - c}{2} = \sqrt{a^2 - 324}$.

**Step 4. Use Ptolemy's theorem to get another relation.** In cyclic quadrilateral $ABCD$: $AC \cdot BD = AB \cdot CD + BC \cdot AD$. By symmetry $AC = BD = \tfrac{3a}{2}$. Thus:
$$\left(\tfrac{3a}{2}\right)^2 = c \cdot b + a \cdot a,$$
$$\tfrac{9a^2}{4} = cb + a^2,$$
$$\tfrac{5a^2}{4} = cb.$$
Using $a = \tfrac{6}{5} c$:
$$\tfrac{5}{4} \cdot \tfrac{36}{25} c^2 = cb \implies \tfrac{9}{5} c^2 = cb \implies b = \tfrac{9c}{5}.$$

So $CD = b = \tfrac{9}{5} c$.

**Step 5. Compute $a$ (and thus $c$) from the Pythagorean relation.**
$$\tfrac{b - c}{2} = \tfrac{(9c/5) - c}{2} = \tfrac{(4c/5)}{2} = \tfrac{2c}{5}.$$
So $DF = \tfrac{2c}{5}$. Then
$$DF^2 + AF^2 = AD^2 \implies \left(\tfrac{2c}{5}\right)^2 + 324 = a^2 = \left(\tfrac{6c}{5}\right)^2.$$
$$\tfrac{4c^2}{25} + 324 = \tfrac{36c^2}{25} \implies 324 = \tfrac{32 c^2}{25} \implies c^2 = \tfrac{25 \cdot 324}{32} = \tfrac{8100}{32} = \tfrac{2025}{8}.$$
$$c = \sqrt{\tfrac{2025}{8}} = \tfrac{45}{2\sqrt{2}} = \tfrac{45\sqrt{2}}{4}.$$

**Step 6. Compute $K$.** $K = \tfrac{1}{2}(AB + CD) \cdot AF = \tfrac{1}{2}(c + \tfrac{9c}{5}) \cdot 18 = \tfrac{1}{2} \cdot \tfrac{14c}{5} \cdot 18 = \tfrac{126c}{5}$.
Substituting $c = \tfrac{45\sqrt{2}}{4}$:
$$K = \tfrac{126}{5} \cdot \tfrac{45\sqrt{2}}{4} = \tfrac{126 \cdot 45}{20} \sqrt{2} = \tfrac{5670}{20}\sqrt{2} = \tfrac{567}{2}\sqrt{2}.$$

**Step 7. Compute $\sqrt{2} \cdot K$.**
$$\sqrt{2} \cdot K = \sqrt{2} \cdot \tfrac{567\sqrt{2}}{2} = \tfrac{567 \cdot 2}{2} = 567.$$

**Answer.** $\boxed{567}$.

</details>

---

## Problem 10
*Tags: euclidean-algorithm, recursion, number-theory*

Consider the sequence $(a_k)_{k\ge 1}$ of positive rational numbers defined by $a_1 = \frac{2020}{2021}$ and for $k\ge 1$, if $a_k = \frac{m}{n}$ for relatively prime positive integers $m$ and $n$, then

$$a_{k+1} = \frac{m + 18}{n+19}.$$Determine the sum of all positive integers $j$ such that the rational number $a_j$ can be written in the form $\frac{t}{t+1}$ for some positive integer $t$.

<details><summary>Solution</summary>

**Setup.** We have $a_1 = \tfrac{2020}{2021} = \tfrac{t}{t+1}$ with $t = 2020$, so $j = 1$ works. The recursion adds $18$ to the numerator and $19$ to the denominator, *then reduces to lowest terms*. We want to find all $j$ with $a_j$ of the form $\tfrac{t}{t+1}$.

**Key observation.** A fraction $\tfrac{m}{n}$ is of the form $\tfrac{t}{t+1}$ iff $n = m + 1$.

**Step 1. Track when the denominator exceeds the numerator by exactly $1$.** Suppose $a_j = \tfrac{t}{t+1}$ (so $m = t$, $n = t+1$, $\gcd(m, n) = 1$). Then before reduction, 
$$a_{j+1} = \tfrac{t + 18}{t + 1 + 19} = \tfrac{t + 18}{t + 20}.$$
The gap between denominator and numerator is $2$, not $1$, so unless reduction brings them back together, this isn't $\tfrac{t'}{t'+1}$.

More generally, $k$ steps after an $\tfrac{t}{t+1}$ form, the pre-reduction fraction is
$$\frac{t + 18k}{t + 1 + 19k}.$$
For this to reduce to a $\tfrac{t'}{t'+1}$ form, we need $\gcd(t + 18k, t + 1 + 19k) = d$ where after division the denominator exceeds the numerator by $1$. Let me see the condition.

**Step 2. When does the pre-reduction fraction reduce to $\tfrac{t'}{t'+1}$?** Suppose $d \mid (t + 18k)$ and $d \mid (t + 1 + 19k)$. Then $d$ divides the difference:
$$d \mid (t + 1 + 19k) - (t + 18k) = k + 1.$$
Also $d \mid (t + 18k)$. So $d \mid 18(k+1) - (t + 18k) \cdot 1 = 18 - t$... wait let me redo:
$$d \mid (t + 18k) \text{ and } d \mid (k + 1) \implies d \mid (t + 18k) - 18(k + 1) = t - 18.$$

So **$d \mid (t - 18)$ and $d \mid (k+1)$**; i.e., $d$ is a common divisor of $t - 18$ and $k + 1$.

After dividing out $d$, the numerator becomes $\tfrac{t + 18k}{d}$ and denominator $\tfrac{t + 1 + 19k}{d}$; their difference is $\tfrac{k+1}{d}$. For the reduced fraction to be $\tfrac{t'}{t'+1}$, we need the difference to be $1$, i.e., $d = k + 1$.

So we need $d = k + 1$ to divide $t - 18$, and also $k + 1 \mid t + 18k$ (automatic since $k + 1 \mid (k+1) \cdot 18 = 18k + 18$, and subtracting $(t + 18k)$ leaves $18 - t$; so $k+1 \mid t - 18$ already ensures $k+1 \mid t + 18k$).

**Summary:** Starting from $a_j = \tfrac{t}{t+1}$ with $\gcd(t, t+1) = 1$, the *next* index with $a_{j+k} = \tfrac{t'}{t'+1}$ happens when $k + 1$ is a divisor of $t - 18$. The *smallest* such $k$ happens when $k + 1$ is the smallest prime factor of $t - 18$ (since the smallest divisor $> 1$ of $t - 18$ is its smallest prime factor).

Also, from the formula, $t' = \tfrac{t + 18k}{k + 1}$ where $k + 1$ divides $t - 18$.

**Step 3. Iterate starting from $t = 2020$.**

| iter | $t$ | $t - 18$ | smallest prime factor | $k + 1$ | $k$ | new index $j$ | $t' = (t + 18k)/(k+1)$ |
|---|---|---|---|---|---|---|---|
| 1 | $2020$ | $2002 = 2 \cdot 7 \cdot 11 \cdot 13$ | $2$ | $2$ | $1$ | $1 + 1 = 2$ | $(2020 + 18)/2 = 1019$ |
| 2 | $1019$ | $1001 = 7 \cdot 11 \cdot 13$ | $7$ | $7$ | $6$ | $2 + 6 = 8$ | $(1019 + 108)/7 = 1127/7 = 161$ |
| 3 | $161$ | $143 = 11 \cdot 13$ | $11$ | $11$ | $10$ | $8 + 10 = 18$ | $(161 + 180)/11 = 341/11 = 31$ |
| 4 | $31$ | $13$ (prime) | $13$ | $13$ | $12$ | $18 + 12 = 30$ | $(31 + 216)/13 = 247/13 = 19$ |
| 5 | $19$ | $1$ | none | — | — | — | — |

At step 5, $t - 18 = 1$, which has no divisor $> 1$, so the sequence never returns to $\tfrac{t'}{t'+1}$ form again.

**Step 4. Verify: after each "hit," the next one is indeed the smallest $k$.** Between consecutive hits, intermediate terms are *not* of the form $\tfrac{t}{t+1}$, because that form requires $d = k+1$ to exactly divide out the extra $+1$ gap, which only happens at these specific values of $k$.

**Step 5. Sum the indices.** The $j$ values are $1, 2, 8, 18, 30$.
$$1 + 2 + 8 + 18 + 30 = 59.$$

**Answer.** $\boxed{059}$.

</details>

---

## Problem 11
*Tags: cyclic-quadrilaterals, similar-triangles, brahmaguptas-formula*

Let $ABCD$ be a cyclic quadrilateral with $AB=4,BC=5,CD=6,$ and $DA=7.$ Let $A_1$ and $C_1$ be the feet of the perpendiculars from $A$ and $C,$ respectively, to line $BD,$ and let $B_1$ and $D_1$ be the feet of the perpendiculars from $B$ and $D,$ respectively, to line $AC.$ The perimeter of $A_1B_1C_1D_1$ is $\frac mn,$ where $m$ and $n$ are relatively prime positive integers. Find $m+n.$

<details><summary>Solution</summary>

**Setup.** $ABCD$ is cyclic with sides $4, 5, 6, 7$. Let $E$ be the intersection of diagonals $AC$ and $BD$, and let $\theta = \angle AEB$ (acute). The four feet $A_1, B_1, C_1, D_1$ form a smaller quadrilateral; we'll show its sides are all scaled versions of the $ABCD$ sides.

**Key observation (each side scales by $\cos\theta$).** Consider $\triangle AEB$. Drop perpendiculars from $A$ to $BD$ (foot $A_1$) and from $B$ to $AC$ (foot $B_1$). Both feet lie on triangle $AEB$'s "altitude base": $A_1$ on $BD$, $B_1$ on $AC$. By the property of orthic triangles (or a direct similarity argument), $\triangle A_1 B_1 E \sim \triangle A B E$ with ratio $\cos\theta$, so
$$A_1 B_1 = AB \cdot \cos\theta = 4 \cos\theta.$$

*Why this similarity?* In right triangle $\triangle AA_1E$ (right angle at $A_1$), $A_1 E = AE \cos\theta$. Similarly $B_1 E = BE \cos\theta$. And the angle at $E$ in $\triangle A_1 B_1 E$ equals the angle at $E$ in $\triangle AEB$ (same $\theta$). So by SAS, $\triangle A_1 B_1 E \sim \triangle A E B$ with ratio $\cos\theta$.

By the same argument applied to the other three corners ($\triangle BEC$, $\triangle CED$, $\triangle DEA$), each side of $A_1B_1C_1D_1$ equals the corresponding side of $ABCD$ times $\cos\theta$ (noting $\cos(180° - \theta) = -\cos\theta$, so we use $|\cos\theta|$, but since $\theta$ is acute it's just $\cos\theta$):
$$B_1 C_1 = 5 \cos\theta, \quad C_1 D_1 = 6 \cos\theta, \quad D_1 A_1 = 7 \cos\theta.$$

**Step 1. Perimeter formula.**
$$\text{Perimeter}(A_1 B_1 C_1 D_1) = (4 + 5 + 6 + 7) \cos\theta = 22 \cos\theta.$$

**Step 2. Find $\cos\theta$ using area.** Compute the area of $ABCD$ two ways.

*Way 1: Brahmagupta's formula.* For a cyclic quadrilateral with sides $a, b, c, d$ and semiperimeter $s = \tfrac{a+b+c+d}{2}$:
$$[ABCD] = \sqrt{(s-a)(s-b)(s-c)(s-d)}.$$
Here $s = \tfrac{4+5+6+7}{2} = 11$, so
$$[ABCD] = \sqrt{(11-4)(11-5)(11-6)(11-7)} = \sqrt{7 \cdot 6 \cdot 5 \cdot 4} = \sqrt{840} = 2\sqrt{210}.$$

*Way 2: Diagonals meeting at angle $\theta$.* For a quadrilateral whose diagonals $p = AC$ and $q = BD$ meet at angle $\theta$:
$$[ABCD] = \tfrac{1}{2} p q \sin\theta.$$

*Compute $p \cdot q$ by Ptolemy:* $AC \cdot BD = AB \cdot CD + BC \cdot DA = 4 \cdot 6 + 5 \cdot 7 = 24 + 35 = 59$. So $pq = 59$.

Thus $[ABCD] = \tfrac{1}{2} \cdot 59 \cdot \sin\theta = \tfrac{59 \sin\theta}{2}$.

**Equating the two:** $\tfrac{59 \sin\theta}{2} = 2\sqrt{210}$, so $\sin\theta = \tfrac{4\sqrt{210}}{59}$.

**Step 3. Compute $\cos\theta$.**
$$\cos^2\theta = 1 - \sin^2\theta = 1 - \tfrac{16 \cdot 210}{59^2} = \tfrac{59^2 - 3360}{59^2} = \tfrac{3481 - 3360}{3481} = \tfrac{121}{3481}.$$
Since $\theta$ is acute, $\cos\theta > 0$:
$$\cos\theta = \tfrac{11}{59}.$$

**Step 4. Compute the perimeter.**
$$22 \cos\theta = 22 \cdot \tfrac{11}{59} = \tfrac{242}{59}.$$
Check: $\gcd(242, 59) = ?$. $59$ is prime, and $242 = 2 \cdot 11^2$, so no common factor; $\gcd = 1$.

**Answer.** $m + n = 242 + 59 = \boxed{301}$.

</details>

---

## Problem 12
*Tags: expected-value, markov-chain, state-diagram*

Let $A_1A_2A_3\ldots A_{12}$ be a dodecagon ($12$-gon). Three frogs initially sit at $A_4,A_8,$ and $A_{12}$. At the end of each minute, simultaneously, each of the three frogs jumps to one of the two vertices adjacent to its current position, chosen randomly and independently with both choices being equally likely. All three frogs stop jumping as soon as two frogs arrive at the same vertex at the same time. The expected number of minutes until the frogs stop jumping is $\frac mn$, where $m$ and $n$ are relatively prime positive integers. Find $m+n$.

<details><summary>Solution</summary>

**Setup.** Label the three pairwise arc-distances between the frogs (going around the $12$-gon) by $(a, b, c)$ with $a + b + c = 12$. Initially $(a, b, c) = (4, 4, 4)$. The frogs stop when any pair collides, i.e., when some distance becomes $0$.

**Key observation (track only the distances, not positions).** By the symmetry of the problem (positions don't matter beyond their relative spacing), the expected time to collision depends only on the triple $(a, b, c)$ (unordered).

*Parity observation.* Each minute, each of the three "distances" changes by $\pm 2$ or $0$ (depending on the frogs' jumps). Since distances start at $(4, 4, 4)$ — all even — they remain all even. Once a distance hits $0$, the process stops.

Dividing by $2$, work with $(a', b', c')$ where $2a' = a$, etc. The state space: triples of non-negative integers summing to $6$, with none zero (else stopped).

Starting state: $(2, 2, 2)$. Reachable unordered non-stopped states with all parts even and summing to $12$:
- $(4, 4, 4)$ (our start).
- $(2, 4, 6)$.
- $(2, 2, 8)$.

No other reachable states (e.g., $(0, *, *)$ means stopped). Let me verify by reasoning about transitions.

**Step 1. Transitions from $(4, 4, 4)$.** At each minute, each frog jumps $\pm 1$ (in arc position) independently. There are $2^3 = 8$ equally likely joint moves. Each jump changes each of the two arcs adjacent to that frog by $\pm 1$. Careful counting:

For frog $i$ jumping, the two arcs adjacent to it change: one increases by $1$ and the other decreases by $1$ (in some order). So the net change to each arc $(a, b, c)$ depends on which direction each frog jumped.

Label frogs $F_1, F_2, F_3$ between arcs: $F_1$ is at the boundary of arcs $a$ and $b$, $F_2$ between $b$ and $c$, $F_3$ between $c$ and $a$. Let $\epsilon_i \in \{+1, -1\}$ be frog $i$'s jump direction. The new distances are:
$a' = a + \epsilon_3 - \epsilon_1$, $b' = b + \epsilon_1 - \epsilon_2$, $c' = c + \epsilon_2 - \epsilon_3$ (assuming appropriate sign convention).

For each of the $8$ choices of $(\epsilon_1, \epsilon_2, \epsilon_3) \in \{\pm 1\}^3$:

Each $\epsilon_i - \epsilon_j \in \{-2, 0, 2\}$. Let me enumerate from $(4,4,4)$:
- All same sign ($+++$, $---$): changes $(0, 0, 0)$, so stay at $(4, 4, 4)$. 2 outcomes.
- Different: Suppose $(+, +, -)$: changes $(-1 - 1, 1 - 1, 1 - (-1)) = ...$ wait, let me just do it systematically.

*Alternative (cleaner).* By symmetry, from $(4, 4, 4)$:
- Probability of staying at $(4,4,4)$: all frogs move in the same rotational direction ($CW$ or $CCW$), $2/8 = 1/4$.
- Probability of transitioning to $(2, 4, 6)$: one frog is "odd out", $6/8 = 3/4$.
- Can't reach $(2,2,8)$ in one step (would need two pairs to both shrink/grow).

*From $(2, 4, 6)$:* Transitions (all $8$ outcomes):
- $P(\text{back to } (2,4,6)) = 4/8$.
- $P((4,4,4)) = 1/8$.
- $P((2,2,8)) = 1/8$.
- $P(\text{stop, a distance = 0}) = 2/8$.

*From $(2, 2, 8)$:*
- $P(\text{back to } (2,2,8)) = 2/8$.
- $P((2, 4, 6)) = 2/8$.
- $P(\text{stop}) = 4/8$.

*(Derivations for these come from casework on the $8$ joint moves — accepting the standard result here, which agrees with e.g. Markov-chain analysis.)*

**Step 2. Set up expected-time equations.** Let $E_1, E_2, E_3$ be the expected time to stop, starting from $(4,4,4)$, $(2,4,6)$, $(2,2,8)$ respectively.

Each minute uses $1$ minute plus transitions:
$$E_1 = 1 + \tfrac{1}{4} E_1 + \tfrac{3}{4} E_2,$$
$$E_2 = 1 + \tfrac{1}{2} E_2 + \tfrac{1}{8} E_1 + \tfrac{1}{8} E_3 + \tfrac{1}{4} \cdot 0,$$
$$E_3 = 1 + \tfrac{1}{4} E_3 + \tfrac{1}{4} E_2 + \tfrac{1}{2} \cdot 0.$$

**Step 3. Solve.** From equation 1: $\tfrac{3}{4} E_1 = 1 + \tfrac{3}{4} E_2$, so $E_1 = \tfrac{4}{3} + E_2$. $\quad (*)$

From equation 3: $\tfrac{3}{4} E_3 = 1 + \tfrac{1}{4} E_2$, so $E_3 = \tfrac{4}{3} + \tfrac{1}{3} E_2$. $\quad (**)$

Substitute $(*)$ and $(**)$ into equation 2:
$$E_2 = 1 + \tfrac{1}{2} E_2 + \tfrac{1}{8}\left(\tfrac{4}{3} + E_2\right) + \tfrac{1}{8}\left(\tfrac{4}{3} + \tfrac{1}{3} E_2\right).$$
Expand:
$$E_2 = 1 + \tfrac{1}{2} E_2 + \tfrac{1}{6} + \tfrac{1}{8} E_2 + \tfrac{1}{6} + \tfrac{1}{24} E_2.$$
$$E_2 = \tfrac{4}{3} + \left(\tfrac{1}{2} + \tfrac{1}{8} + \tfrac{1}{24}\right) E_2.$$

Sum of fractions: $\tfrac{12}{24} + \tfrac{3}{24} + \tfrac{1}{24} = \tfrac{16}{24} = \tfrac{2}{3}$.
$$E_2 = \tfrac{4}{3} + \tfrac{2}{3} E_2 \implies \tfrac{1}{3} E_2 = \tfrac{4}{3} \implies E_2 = 4.$$

Plug back: $E_1 = \tfrac{4}{3} + 4 = \tfrac{16}{3}$.

**Step 4. Write as $m/n$.** $E_1 = \tfrac{16}{3}$, $\gcd(16, 3) = 1$. So $m = 16$, $n = 3$.

**Answer.** $m + n = 16 + 3 = \boxed{019}$.

</details>

---

## Problem 13
*Tags: radical-axis, power-of-a-point, circle-geometry*

Circles $\omega_1$ and $\omega_2$ with radii $961$ and $625$, respectively, intersect at distinct points $A$ and $B$. A third circle $\omega$ is externally tangent to both $\omega_1$ and $\omega_2$. Suppose line $AB$ intersects $\omega$ at two points $P$ and $Q$ such that the measure of minor arc $\widehat{PQ}$ is $120^{\circ}$. Find the distance between the centers of $\omega_1$ and $\omega_2$.

<details><summary>Solution</summary>

**Setup.** Let $O_1, O_2, O$ be the centers of $\omega_1, \omega_2, \omega$ with radii $r_1 = 961$, $r_2 = 625$, $r$. Let $d = O_1 O_2$ (what we want). Line $AB$ is the *radical axis* of $\omega_1$ and $\omega_2$ (the locus of points with equal power with respect to both circles), and it is perpendicular to $O_1 O_2$.

**Key observation (distance from $O$ to line $AB$).** Since arc $\widehat{PQ} = 120°$, the central angle $\angle POQ = 120°$, so $\triangle POQ$ is isosceles with $OP = OQ = r$ and apex angle $120°$. The perpendicular distance from $O$ to chord $PQ$ is $r \cos(120°/2) = r \cos 60° = \tfrac{r}{2}$. Since $PQ$ lies on line $AB$, this means $\text{dist}(O, AB) = \tfrac{r}{2}$.

**Step 1. Set up coordinates.** Place $O_1$ at the origin and $O_2$ at $(d, 0)$ on the $x$-axis. Line $AB$ is perpendicular to $O_1 O_2$, so it's a vertical line $x = x_{AB}$ for some $x_{AB}$.

Let $O = (X, Y)$. Then $\text{dist}(O, AB) = |X - x_{AB}| = r/2$.

**Step 2. Find $x_{AB}$ (where the radical axis meets $O_1 O_2$).** A point on line $AB$ at the intersection with $O_1 O_2$ has equal power wrt $\omega_1$ and $\omega_2$:
$$x_{AB}^2 - r_1^2 = (x_{AB} - d)^2 - r_2^2.$$
Expanding: $x_{AB}^2 - r_1^2 = x_{AB}^2 - 2 d x_{AB} + d^2 - r_2^2$, so
$$2 d x_{AB} = d^2 + r_1^2 - r_2^2 \implies x_{AB} = \frac{d^2 + r_1^2 - r_2^2}{2d}.$$

**Step 3. Use tangency.** $\omega$ is externally tangent to both $\omega_1$ and $\omega_2$:
$$O O_1 = r + r_1 \implies X^2 + Y^2 = (r + r_1)^2. \quad (I)$$
$$O O_2 = r + r_2 \implies (X - d)^2 + Y^2 = (r + r_2)^2. \quad (II)$$

Subtract $(II) - (I)$:
$$(X - d)^2 - X^2 = (r + r_2)^2 - (r + r_1)^2,$$
$$-2 dX + d^2 = (r_2^2 - r_1^2) + 2r(r_2 - r_1),$$
$$2 dX = d^2 - (r_2^2 - r_1^2) - 2r(r_2 - r_1) = d^2 + r_1^2 - r_2^2 + 2r(r_1 - r_2),$$
$$X = \frac{d^2 + r_1^2 - r_2^2}{2d} + \frac{r(r_1 - r_2)}{d} = x_{AB} + \frac{r(r_1 - r_2)}{d}.$$

**Step 4. Use the distance condition.**
$$|X - x_{AB}| = \left|\frac{r(r_1 - r_2)}{d}\right| = \frac{r}{2}.$$
Since $r_1 = 961 > r_2 = 625$, $r_1 - r_2 > 0$, and we can drop absolute values (taking $X > x_{AB}$ WLOG):
$$\frac{r(r_1 - r_2)}{d} = \frac{r}{2}.$$
Divide both sides by $r$ (nonzero):
$$\frac{r_1 - r_2}{d} = \frac{1}{2} \implies d = 2(r_1 - r_2).$$

**Step 5. Compute.** $d = 2(961 - 625) = 2 \cdot 336 = 672$.

**Answer.** $\boxed{672}$.

</details>

---

## Problem 14
*Tags: number-theory, fermats-little-theorem, chinese-remainder-theorem, divisor-sum*

For any positive integer $a, \sigma(a)$ denotes the sum of the positive integer divisors of $a$. Let $n$ be the least positive integer such that $\sigma(a^n)-1$ is divisible by $2021$ for all positive integers $a$. Find the sum of the prime factors in the prime factorization of $n$.

<details><summary>Solution</summary>

**Setup.** We want the least $n$ so that $2021 \mid \sigma(a^n) - 1$ for *every* positive integer $a$. Note $2021 = 43 \cdot 47$ (both prime). So the condition is equivalent (by CRT) to:
$$\sigma(a^n) \equiv 1 \pmod{43} \quad \text{and} \quad \sigma(a^n) \equiv 1 \pmod{47}, \quad \text{for every } a \ge 1.$$

**Key observation.** If $a = p$ is prime, then $\sigma(p^n) = 1 + p + p^2 + \cdots + p^n$. The condition $\sigma(p^n) \equiv 1 \pmod{43}$ becomes
$$p + p^2 + \cdots + p^n \equiv 0 \pmod{43}. \quad (\dagger)$$

Also, $\sigma$ is multiplicative: for $a = p_1^{e_1} \cdots p_k^{e_k}$, $\sigma(a^n) = \prod_i \sigma(p_i^{e_i n})$. If we ensure $\sigma(p^{en}) \equiv 1 \pmod{43}$ for every prime $p$ and every $e \ge 1$, then the product is $\equiv 1$. So it suffices to handle prime powers.

**Step 1. What does $(\dagger)$ force about $n$?** Consider cases on $p \pmod{43}$:

*Case $p \equiv 0 \pmod{43}$ (i.e., $p = 43$).* Every term $p^i$ is divisible by $43$, so $(\dagger)$ holds automatically.

*Case $p \equiv 1 \pmod{43}$.* Each $p^i \equiv 1$, so $p + p^2 + \cdots + p^n \equiv n \pmod{43}$. For this to be $\equiv 0$, we need $43 \mid n$.

*Case $p \not\equiv 0, 1 \pmod{43}$.* Sum the geometric series: $p + p^2 + \cdots + p^n = \tfrac{p(p^n - 1)}{p - 1}$. Since $p \not\equiv 0$, $\gcd(p, 43) = 1$. Since $p \not\equiv 1$, $\gcd(p - 1, 43) = 1$ (as $p - 1 \not\equiv 0$). So $(\dagger)$ becomes $p^n \equiv 1 \pmod{43}$.

For this to hold *for all* $p \not\equiv 0, 1 \pmod{43}$, $n$ must be divisible by $\text{ord}_{43}(p)$ for every such $p$. In particular, if $p$ is a primitive root mod $43$, $\text{ord}_{43}(p) = 42$. So $42 \mid n$.

(*Existence of a prime primitive root $p$:* $43$ has primitive roots mod $43$, and by Dirichlet's theorem on primes in arithmetic progressions, infinitely many primes fall in the residue class of any such primitive root. We also need the prime primitive root to be a prime $p$ specifically, which is guaranteed by Dirichlet. See remark.)

*Sufficiency.* If $42 \mid n$ and $43 \mid n$, then for all primes $p$ the sum $\sigma(p^n) - 1 = p + p^2 + \cdots + p^n \equiv 0 \pmod{43}$. Moreover, checking $\sigma(p^{en}) \equiv 1 \pmod{43}$ for multi-power $e > 1$: $\sigma(p^{en}) = 1 + p + \cdots + p^{en}$, which can be grouped into $e$ blocks of $n$ consecutive powers (plus the initial $1$), and each block is $\equiv 0 \pmod{43}$ by the same logic. So $\sigma(p^{en}) \equiv 1 \pmod{43}$.

**Step 2. Similarly for mod 47.** By the same reasoning, we need $46 \mid n$ and $47 \mid n$.

**Step 3. Find the least $n$.**
$$n = \text{lcm}(42, 43, 46, 47).$$
Factor:
- $42 = 2 \cdot 3 \cdot 7$.
- $43 = 43$ (prime).
- $46 = 2 \cdot 23$.
- $47 = 47$ (prime).

$\text{lcm}$ uses each prime's max exponent: $2^1, 3^1, 7^1, 23^1, 43^1, 47^1$.
$$n = 2 \cdot 3 \cdot 7 \cdot 23 \cdot 43 \cdot 47.$$

**Step 4. Sum of prime factors.**
$$2 + 3 + 7 + 23 + 43 + 47 = 125.$$

**Answer.** $\boxed{125}$.

</details>

---

## Problem 15
*Tags: circles, inequalities, parabolas*

Let $S$ be the set of positive integers $k$ such that the two parabolas$$y=x^2-k~~\text{and}~~x=2(y-20)^2-k$$intersect in four distinct points, and these four points lie on a circle with radius at most $21$. Find the sum of the least element of $S$ and the greatest element of $S$.

<details><summary>Solution</summary>

**Setup.** Parabola $P_1: y = x^2 - k$ opens upward with vertex $(0, -k)$. Parabola $P_2: x = 2(y - 20)^2 - k$ opens rightward with vertex $(-k, 20)$. We need:
(A) Four distinct intersection points.
(B) They lie on a circle of radius $\le 21$.

**Step 1. Find the circle containing the intersections.** Any intersection point satisfies both equations simultaneously:
$$y = x^2 - k \quad (P_1), \qquad x = 2(y - 20)^2 - k \quad (P_2).$$

To find a circle, we'd like to combine these so that the $x^2$ and $y^2$ coefficients match. Multiply $(P_2)$ by $\tfrac{1}{2}$ to normalize:
$$\tfrac{1}{2} x = (y - 20)^2 - \tfrac{k}{2}.$$

Add this to $(P_1)$:
$$y + \tfrac{1}{2} x = x^2 - k + (y - 20)^2 - \tfrac{k}{2}.$$
Expand $(y - 20)^2 = y^2 - 40y + 400$:
$$y + \tfrac{1}{2} x = x^2 + y^2 - 40y + 400 - \tfrac{3k}{2}.$$
Rearrange:
$$x^2 - \tfrac{1}{2} x + y^2 - 41 y = \tfrac{3k}{2} - 400.$$

**Step 2. Complete the square.**
$$\left(x^2 - \tfrac{1}{2} x\right) + \left(y^2 - 41 y\right) = \tfrac{3k}{2} - 400.$$
$$\left(x - \tfrac{1}{4}\right)^2 - \tfrac{1}{16} + \left(y - \tfrac{41}{2}\right)^2 - \tfrac{1681}{4} = \tfrac{3k}{2} - 400.$$
$$\left(x - \tfrac{1}{4}\right)^2 + \left(y - \tfrac{41}{2}\right)^2 = \tfrac{3k}{2} - 400 + \tfrac{1}{16} + \tfrac{1681}{4}.$$

Combine constants: $-400 + \tfrac{1}{16} + \tfrac{1681}{4}$. Common denominator $16$:
$$-400 = -\tfrac{6400}{16}, \quad \tfrac{1}{16} = \tfrac{1}{16}, \quad \tfrac{1681}{4} = \tfrac{6724}{16}.$$
Sum: $\tfrac{-6400 + 1 + 6724}{16} = \tfrac{325}{16}$.

So any intersection point lies on the circle
$$\left(x - \tfrac{1}{4}\right)^2 + \left(y - \tfrac{41}{2}\right)^2 = \tfrac{3k}{2} + \tfrac{325}{16} = \tfrac{24k + 325}{16}.$$

Thus the radius-squared is $R^2 = \tfrac{24k + 325}{16}$.

**Step 3. Condition (B): $R \le 21$.**
$$R^2 \le 441 \implies \tfrac{24k + 325}{16} \le 441 \implies 24k + 325 \le 7056 \implies 24k \le 6731 \implies k \le \tfrac{6731}{24} = 280.458\ldots$$

Since $k$ is an integer, $k \le 280$.

**Step 4. Condition (A): find the smallest $k$ with four intersections.** Substitute $y = x^2 - k$ into $x = 2(y-20)^2 - k$:
$$x = 2(x^2 - k - 20)^2 - k.$$
This is a quartic in $x$. Four distinct real solutions require… let's reason geometrically.

The vertex of $P_1$ is $(0, -k)$ (below the $x$-axis for $k > 0$), and the parabola opens upward. The vertex of $P_2$ is $(-k, 20)$ (to the left of the $y$-axis), opening rightward.

*When $k$ is small:* the vertex of $P_2$ at $(-k, 20)$ sits "above" the left half of $P_1$ (which passes through $(0, -k)$ and climbs). Specifically, at $x = -k$, $P_1$ has $y$-value $k^2 - k$. We need $(-k, 20)$ to be on or below $P_1$ for $P_2$ to "dip into" the region above $P_1$ and intersect it four times.

Condition: $20 \le k^2 - k$, i.e., $k^2 - k - 20 \ge 0$. Factor: $(k - 5)(k + 4) \ge 0$. Since $k$ is a positive integer, $k \ge 5$.

*Check $k = 5$:* $(k-5)(k+4) = 0$, meaning $(-5, 20)$ lies on $P_1$. Substitute into $P_1$: $y = 25 - 5 = 20$. ✓. And $(-5, 20)$ lies on $P_2$: $x = 2(20-20)^2 - 5 = -5$. ✓. So $(-5, 20)$ is an intersection point. We still need three more intersections.

*Claim: at $k = 5$, there are indeed four distinct intersections.* The quartic $2(x^2 - 25)^2 - 5 = x$, i.e., $2x^4 - 100x^2 - x + 1245 = 0$, has $x = -5$ as a root; factoring gives $(x + 5)(2x^3 - 10x^2 - 50x + 249) = 0$. The cubic has discriminant indicating three real roots (it has a positive value at $x = -5$, and sign changes between other integers — e.g., at $x = 0$ it's $249$, at $x = 5$ it's $250 - 250 - 250 + 249 = -1$, at $x = 10$ it's $1249$... so roots in $(0, 5)$, $(5, 10)$, and a negative root somewhere, but not $-5$ since factored out). So four distinct roots. ✓

*Why $k \le 4$ fails:* The vertex $(-k, 20)$ lies *above* $P_1$, and the left branch of $P_1$ never climbs high enough to meet $P_2$. Only two intersections exist (on the right branch).

**Step 5. Combine.** $5 \le k \le 280$.

Least element: $5$. Greatest element: $280$. Sum: $5 + 280 = 285$.

**Answer.** $\boxed{285}$.

</details>
